name: Server CI

on:
  pull_request:
    branches: [main, develop]
    paths:
      - "server/**"
      - "server/prisma/**"
      - ".github/workflows/server-ci.yml"
  push:
    branches: [main, develop]
    paths:
      - "server/**"
      - "server/prisma/**"
      - ".github/workflows/server-ci.yml"

concurrency:
  group: server-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  server-check:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: server

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      # Prisma client 생성이 깨지는지 확인
      - name: Prisma generate
        run: npx prisma generate
        env:
          # generate 자체는 보통 DB 연결이 필요 없지만,
          # schema가 env를 강제하거나 postinstall 훅이 있으면 필요할 수 있어 더미로 둠
          DATABASE_URL: ${{ secrets.TEST_DATABASE_URL }}

      # (선택) 타입체크/린트/테스트가 있으면 주석 해제해서 추가
      # - name: Typecheck
      #   run: npm run typecheck
      # - name: Lint
      #   run: npm run lint
      # - name: Test
      #   run: npm test
